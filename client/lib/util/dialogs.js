
/**
 * Asks the user whether to save the diagram before close.
 *
 * Callback is invoked with (err, { discard, cancel, save });
 *
 * @param {DiagramFile} diagram
 * @param {Function} callback
 */
module.exports.askSaveBeforeClose = function(diagram, callback) {

};